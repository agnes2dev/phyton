#  Triângulo Válido
#Peça três lados de um triângulo e verifique se eles podem formar um triângulo
#(cada lado deve ser menor que a soma dos outros dois).

print("Sistema de cálculos de triangulos")

lado1 = float(input(f"Digite o tamanho do primeiro lado:  "))
lado2 = float(input(f"Digite o tamanho do segundo lado:  "))
lado3 = float(input(f"Digite o tamanho do terceiro lado:  "))

resultado = lado1 + lado2 + lado3

soma1 = lado1 + lado2


if (soma1<= lado3) and resultado < 180:
    print("Os lados não podem formar um triângulo a soma de dois lados do triâgulo deve ser maior que o terceiro lado, e obter obrigatoriamente 180º graus nessa soma.")
    
else:
    print(f"Os valores formam um triângulo válido. A soma dos lados é de {resultado}º graus.") 

